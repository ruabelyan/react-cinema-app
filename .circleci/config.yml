version: 2.1

orbs:
  node: circleci/node@4.1


jobs:
  build-and-test:  
    executor:
      name: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Run tests
          command: npm test

workflows:
  sample: 
    jobs:
      - build-and-test
